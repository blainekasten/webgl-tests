;(function(window){
  var scene;

  window.define('scene', ['vendor/three'], function(THREE) {
    //if (scene){
      //return scene;
    //}

    scene = new THREE.Scene()
    return scene;
  });
})(this);
